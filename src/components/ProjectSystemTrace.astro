<figure
  class="system-trace trace-surface project-trace"
  role="img"
  aria-label="Abstract System Trace composition inspired by connected industrial operations. It does not represent the project's actual architecture."
>
  <div class="trace-header" aria-hidden="true">
    <span class="trace-annotation">System trace / 01</span>
    <span class="trace-annotation">Abstract domain study</span>
  </div>

  <svg viewBox="0 0 640 400" aria-hidden="true">
    <g class="structural-paths">
      <path d="M64 92H222V64H382V112H576" />
      <path d="M64 198H164V146H306V238H452V184H576" />
      <path d="M64 306H256V278H386V322H576" />
      <path d="M164 92V306" />
      <path d="M306 64V278" />
      <path d="M452 112V322" />
    </g>

    <path
      class="active-path"
      d="M64 306H164V198H306V146H452V112H576"
    />

    <g class="structural-nodes">
      <rect x="60" y="88" width="8" height="8" />
      <rect x="218" y="60" width="8" height="8" />
      <rect x="378" y="108" width="8" height="8" />
      <rect x="572" y="180" width="8" height="8" />
      <rect x="252" y="274" width="8" height="8" />
      <rect x="382" y="318" width="8" height="8" />
    </g>

    <g class="active-nodes">
      <rect x="60" y="302" width="8" height="8" />
      <rect x="160" y="194" width="8" height="8" />
      <rect x="302" y="142" width="8" height="8" />
      <rect x="448" y="108" width="8" height="8" />
      <rect x="572" y="108" width="8" height="8" />
    </g>

    <g class="trace-labels">
      <text x="64" y="78">A1</text>
      <text x="222" y="50">B2</text>
      <text x="382" y="98">C1</text>
      <text x="164" y="326">A3</text>
      <text x="306" y="258">B4</text>
      <text x="452" y="342">C3</text>
      <text x="548" y="98">D1</text>
    </g>
  </svg>

  <figcaption class="trace-annotation">Structure / connection / state</figcaption>
</figure>

<style>
  .project-trace {
    display: grid;
    min-height: 18rem;
    padding: 1rem;
  }

  .trace-header {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
  }

  svg {
    width: 100%;
    height: auto;
    align-self: center;
  }

  path,
  rect {
    vector-effect: non-scaling-stroke;
  }

  .structural-paths path {
    fill: none;
    stroke: var(--trace-structure);
    stroke-width: 1;
  }

  .active-path {
    fill: none;
    stroke: var(--trace-active);
    stroke-width: 2;
    transition: stroke-width 180ms var(--ease-out);
  }

  .structural-nodes rect {
    fill: var(--canvas);
    stroke: var(--trace-structure);
    stroke-width: 1;
  }

  .active-nodes rect {
    fill: var(--canvas);
    stroke: var(--trace-active);
    stroke-width: 2;
    transition: fill 180ms var(--ease-out);
  }

  .trace-labels {
    fill: var(--faint);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 11px;
  }

  figcaption {
    align-self: end;
  }

  @media (hover: hover) and (pointer: fine) {
    :global(.featured-project:hover) .active-path {
      stroke-width: 3;
    }

    :global(.featured-project:hover) .active-nodes rect {
      fill: var(--trace-active);
    }
  }

  @media (min-width: 48rem) {
    .project-trace {
      min-height: 24rem;
      padding: 1.25rem;
    }
  }
</style>
